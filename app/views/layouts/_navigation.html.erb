<nav class="navigation">
  <div class="container flex justify-between items-center">
    <div class="left-nav">
      <% if current_user %>
        <span><%= current_user.role.titleize %>: <%= current_user.full_name %></span>
        &nbsp;|&nbsp;
        <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to logout?" } %>
      <% else %>
        <%= link_to "Login", user_session_path %>
        <%= link_to "Sign Up", new_user_registration_path %>
      <% end %>
    </div>
    <div class="right-nav">
      <%= link_to "Home", root_path %>
      <%= link_to "Manage Users", aurelius_press_admin_users_path %>
    </div>
  </div>
</nav>
