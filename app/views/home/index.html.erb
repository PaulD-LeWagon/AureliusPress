<div class="content">

  <%= render HeroComponent.new(title: @title, subtitle: @subtitle) %>

  <div class="container text-center">

    <h2 class="mt-10 mb-5"><%= @description.shift %></h2>

    <% @description.each do |paragraph| %>
      <p><%= paragraph %></p>
    <% end %>

  </div>

</div>
